import { shallowMount } from '@vue/test-utils'
import App from '@/App.vue'

test('Expect 1 + 1 = 2', () => {

  // expect the result
  // of 1+1 to be 2
  expect(1+1).toBe(2)
})

//test('App exists', () => {

  // expect the result
  // of 1+1 to be 2
//  expect(App).toBe(App)
//})
